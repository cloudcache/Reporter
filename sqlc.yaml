version: "2"
sql:
  - engine: "mysql"
    queries: "internal/store/query"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/store/db"
        emit_json_tags: true
        emit_prepared_queries: true
